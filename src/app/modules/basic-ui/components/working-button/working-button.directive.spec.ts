import { WorkingButtonDirective } from './working-button.directive';

describe('WorkingButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkingButtonDirective();
    expect(directive).toBeTruthy();
  });
});
